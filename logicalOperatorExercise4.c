#include <stdio.h>

int main()
{
    int one;
    printf("Enter the number to multiplay it with 7 twice:\n");
    printf("Enter the number:");
    scanf("%d",&one);
    
    printf("%d",one*7*7);

    return 0;
}
